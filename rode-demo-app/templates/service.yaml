apiVersion: v1
kind: Service
metadata:
  name: {{ include "rode-demo-app.fullname" . }}
  labels:
    {{- include "rode-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rode-demo-app.selectorLabels" . | nindent 4 }}
